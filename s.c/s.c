#include <stdio.h>

int main(void)
{
  long long int a;
  scanf("%lld", &a);
  printf("%lld",a+1);
	return 0;
}

echo; echo 'eval "$(/opt/homebrew/bin/brew shellenv)"') >> /Users/kmj1110/.zprofile
    eval "$(/opt/homebrew/bin/brew shellenv)"
